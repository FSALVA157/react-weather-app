import { apiWeather } from "../apis/apiWeather";

export default class Request{

    static getData(endpoint){
        // return async(endpoint)=>{
        //      const info = await apiWeather.get(endpoint)
        //      return info;
        // }
    }
}